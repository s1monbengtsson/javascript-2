import Container from "react-bootstrap/Container"

const HomePage = () => {
	return (
		<Container className="py-3">
			<h1>Welcome!</h1>

			<p>This is a clone of <code>16-redux-toolkit</code> but it has React Testing Library installed and a few modified components.</p>
		</Container>
	)
}

export default HomePage
